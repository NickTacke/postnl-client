pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,json}"
      run: bunx biome check --staged --no-errors-on-unmatched {staged_files}
commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
